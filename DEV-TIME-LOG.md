# Development Time Log — Track2 (Time Tracking & Invoicing Platform)

A running log of actual development and working-session time on the Track2 project, kept so hours can be pulled by date and later entered into our time tracker. Maps to the R&D project **"R&D – Track2 Time Tracking & Invoicing Platform."**

**How to read this:** one row per working session. `Hours` for past sessions are **estimates reconstructed from git commit activity** (commit timestamps + scope of work in each window) — treat them as a starting point and adjust to actual where known. New sessions are appended as they happen.

**R&D task categories** (per the R&D Tax Credit Tracking Guide): Research & Discovery · Technical Design & Architecture · Development / Configuration · Testing & QA · Debugging & Iteration. When entering into the tracker, exclude non-qualifying time (learning, routine styling, PM) per the guide.

| Date | Session | Hours (est.) | R&D task category | Notes |
|---|---|---:|---|---|
| 2026-08-04 | Discovery & spec set | 1.5 | Research & Discovery | Defined the platform scope and wrote the testable specification/acceptance-criteria set that drove the build. |
| 2026-08-04 | Foundation build | 5.0 | Technical Design & Architecture; Development | Data model + Prisma schema + deterministic seed; core business-logic modules with unit tests (effective-dated rate resolution, integer-cents money math, duration math, invoice totals, invoice state machine, permission/capability layer); provisioned the Postgres database; scaffolded the Next.js app + first live screens. |
| 2026-08-05 | Core billing loop | 0.5 | Development / Configuration | Completed the track → invoice → send → record-payment loop; first reporting screens. |
| 2026-08-05 | Auth & multi-tenancy | 2.5 | Technical Design & Architecture; Development | Session-based authentication; per-account (multi-tenant) data scoping; adopted real database migrations; entity create forms. |
| 2026-08-05 | Entity CRUD | 1.0 | Development / Configuration | Edit forms for core entities; expense entry + categories; inline timesheet editing. |
| 2026-08-06 | Modules & billing features | 2.0 | Development / Configuration; Testing & QA | Account settings + feature-module gating; billing expenses onto invoices; prepaid retainers; recurring-invoice profiles + scheduled generation; estimates + conversion; automated tenant-isolation test suite. |
| 2026-08-06 | Integrations & reporting depth | 2.5 | Development / Configuration; Testing & QA; Debugging & Iteration | Third-party integrations (online payments, accounting sync, project-management import) with an encrypted-at-rest credential store + admin UI; signature-verified idempotent payment webhooks; reporting period/grouping depth; project team assignment; shared time-entry interface. |
| 2026-08-07 | Invoice-detail gaps + team detail/permissions + fidelity re-audit | 2.0 | Development / Configuration; Testing & QA; Debugging & Iteration | Invoice activity/history log, Actions menu (write-off/duplicate/resend/manual accounting-sync), edit + blank composer, print/PDF; team-member detail hub with granular per-user permission overrides enforced across all call sites; full second-pass fidelity audit cataloguing correctness bugs vs feature gaps, then a correctness-bug cleanup pass. |
| 2026-08-07 | Outbound email + fidelity batch + search + invoice Configure + multi-brand console | 2.5 | Technical Design & Architecture; Development / Configuration | Transactional email pipeline (provider + encrypted key UI; invoice-sent, receipt, overdue-reminder templates); autonomous batch (delete payment / delete sent invoice, per-project person/task rates, expense edit-delete-receipts, archive/restore + client contacts, reports depth + dashboard KPIs); global cross-entity search; invoice Configure suite (renameable field labels, editable messages, item types, sender addresses); per-company brand console (per-entity branding/accent, sender identity, email theming + language) with schema + resolver work. |
| 2026-08-07 | Harvest→Track2 production data migration (tooling + execution) | 3.0 | Development / Configuration; Testing & QA; Debugging & Iteration | Staged, resumable, idempotent ETL runner with per-entity reconciliation + guarded demo-data clear; diagnosed and fixed a stale long-lived DB-connection stall by making the 388k-row time-entry import resumable with a fresh connection per year-part; billing pass importing invoices/expenses with reconstructed activity/status timelines back-dated from source timestamps; captured the real project-assignment roster (PM flags + rates) via rate-limited API sync. (Excludes background import run-time.) |
| 2026-08-07 | Real-data screen audits + rebuilds | 2.0 | Development / Configuration; Debugging & Iteration | With the full dataset loaded, re-audited and rebuilt core screens to aggregate in the database instead of loading hundreds of thousands of rows: project detail (chart + KPI cards + tabbed task breakdown), team members week view (utilization vs capacity), and the projects / clients / tasks / invoices lists (filters, sort, pagination); shared reference-line column chart. |
| 2026-08-08 | Reports scale fix + audit consolidation | 1.0 | Debugging & Iteration; Development / Configuration | Rewrote the Time and Profitability reports to aggregate in the database (were loading every time entry into memory — fatal at full history); consolidated the open UI loose-ends + migration follow-ups into tracked spec files. |
| 2026-08-08 | Mobile responsive shell + progress/time reporting | 1.25 | Technical Design & Architecture; Development / Configuration | Responsive app shell (off-canvas sidebar drawer + hamburger below the desktop breakpoint, responsive spacing) so the app is usable on a phone; authored the mobile/responsive spec; refreshed the build-progress doc and this time log. |

**Running total (estimated): 26.75 hours** _(through 2026-08-08; +11.75 since 2026-08-06)_

---

### Appending new sessions
Add a row with: date, a short session name, estimated hours, the R&D task category, and a one-line technical note (what was built/tested and the uncertainty being worked through). Keep notes specific enough to be defensible for R&D review, e.g. _"Testing idempotency of inbound payment webhook against duplicate delivery."_
