# Copy to .env and adjust for your local Postgres.
# The clone is multi-tenant Postgres (see specs/00-overview.md).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/harvest_clone?schema=public"
